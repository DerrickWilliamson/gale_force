import 'package:flutter/material.dart';

class WeatherRepo extends StatelessWidget {
  const WeatherRepo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
